from django.conf.urls.defaults import *

urlpatterns = patterns('',
    # Example:
    # (r'^test_standardapps/', include('test_standardapps.foo.urls')),

    # Uncomment this for admin:
#     (r'^admin/', include('django.contrib.admin.urls')),
)
